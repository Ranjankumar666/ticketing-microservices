name: Tests-Auth
on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Running the auth services tests"
      - run: cd auth && npm install && npm run test:ci
      
