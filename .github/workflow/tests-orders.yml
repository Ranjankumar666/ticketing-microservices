name: Tests-Orders
on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Running the orders services tests"
      - run: cd orders && npm install && npm run test:ci
      
